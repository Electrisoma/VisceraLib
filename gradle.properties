# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
#org.gradle.parallel=true
org.gradle.caching=true
org.gradle.caching.debug=false
#org.gradle.configureondemand=true

# Mod properties
mod.version=0.1.0
mod.group=net.electrsoma
mod.id=resotech
mod.name=ResoTech
mod.description=Compose a factory.
mod.authors=electrsoma
mod.license=MIT

mod.mc_dep_fabric=[VERSIONED]
mod.mc_dep_forgelike=[VERSIONED]
mod.mc_title=[VERSIONED]
mod.mc_targets=[VERSIONED]

# Mod setup
deps.mixin_extras=0.4.1
deps.fabric_loader=0.16.10
deps.fabric_api_version=[VERSIONED]
deps.forge_loader=[VERSIONED]
deps.neoforge_loader=[VERSIONED]
deps.neoforge_patch=[VERSIONED]

# Mod dependencies
deps.parchment_version=[VERSIONED]

# Integration
deps.archApi=[VERSIONED]
deps.archApi_range_forge=[VERSIONED]
deps.archApi_range_fabric=[VERSIONED]
deps.portingLib=[VERSIONED]

deps.flywheel=[VERSIONED]
deps.flywheel_range_forge=[VERSIONED]
deps.flywheel_range_fabric=[VERSIONED]

deps.veil=[VERSIONED]
deps.veil_range_forge=[VERSIONED]
deps.veil_range_fabric=[VERSIONED]

deps.ponder=[VERSIONED]
deps.ponder_range_forge=[VERSIONED]
deps.ponder_range_fabric=[VERSIONED]

# Utilities
deps.modmenu=[VERSIONED]

# Publishing
publish.modrinth=...
publish.curseforge=...