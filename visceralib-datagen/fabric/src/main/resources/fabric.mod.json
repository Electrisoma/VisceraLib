{
  "schemaVersion": 1,
  "id": "${id}_${module}",
  "version": "${version}",
  "name": "${name}-${moduleCaps}",
  "description": "Contains code for modloader agnostic data generation utilities.",
  "authors": [
    "${authors}"
  ],
  "contact": {
  },
  "license": "${license}",
  "icon": "logo.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "${group}.${id}.impl.${module}.EntrypointFabric"
    ],
    "client": [
      "${group}.${id}.impl.${module}.ClientEntrypointFabric"
    ]
  },
  "mixins": [
    "${id}_${module}-common.mixins.json",
    "${id}_${module}-fabric.mixins.json"
  ],
  "depends": {
    "visceralib_core": "*",
    "fabricloader": ">=${fabric}",
    "fabric-api-base": "*",
    "fabric-data-generation-api-v1": "*",
    "fabric-convention-tags-v2": "*",
    "minecraft": "${minecraft}",
    "java": ">=${java}"
  },
  "custom": {
    "modmenu": {
      "badges": [ "library" ],
      "parent": {
        "id": "${id}",
        "name": "${name}",
        "description": "${description}",
        "icon": "logo.png",
        "badges": [ "library" ]
      },
      "update_checker": true
    }
  }
}
